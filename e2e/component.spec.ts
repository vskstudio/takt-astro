// Exercises the <Takt /> component install path — the other public entry, which
// ships as Takt.astro and must resolve its imports from dist/ (not src/).
import { contractTests } from './contract'

contractTests()
